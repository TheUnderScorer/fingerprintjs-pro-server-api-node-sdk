name: Release
on:
  push:
    branches:
      - main
      - test

jobs:
  build-and-release:
    name: 'Build project, run CI checks and publish new release'
    uses: fingerprintjs/dx-team-toolkit/.github/workflows/release-sdk-changesets.yml@bugfixfix-changesets-workflow
    with:
      branch: ${{ github.ref }}
    secrets:
      GH_RELEASE_TOKEN: ${{ secrets.GITHUB_TOKEN }}
